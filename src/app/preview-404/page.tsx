import type { Metadata } from 'next';
import NotFound from '../not-found';

/**
 * پیش‌نمایش صفحه‌ی ۴۰۴.
 *
 * صفحه‌ی واقعی ۴۰۴ فقط وقتی دیده می‌شود که آدرسی اشتباه باشد، و آن
 * موقع نمی‌شود راحت ویرایشش کرد. این مسیر همان کامپوننت را نشان
 * می‌دهد تا بدون شکستن چیزی قابل دیدن و تنظیم باشد.
 *
 * از ایندکس بیرون است — یک صفحه‌ی ۴۰۴ که با کد ۲۰۰ سرو شود، برای
 * گوگل «محتوای تکراری بی‌ارزش» است.
 *
 * اسم پوشه عمداً زیرخط ندارد: در App Router هر پوشه‌ای که با _ شروع
 * شود پوشه‌ی خصوصی حساب می‌شود و اصلاً روت نمی‌سازد.
 */
export const metadata: Metadata = {
  title: 'پیش‌نمایش صفحه‌ی ۴۰۴',
  robots: { index: false, follow: false },
};

export default function Preview404() {
  return <NotFound />;
}
